{
  "_comment": "BE-only scenario. The browser always submits the country belonging to the locale page it is on, so a country override cannot be expressed through the UI — the FE would perform an ordinary valid login and report a false failure. fe_applicable is false so the FE layer records a skip instead. See PLAN.md section 5.",
  "name": "S1_mismatched_country",
  "description": "Valid KOR credentials submitted with country='usa'. A success here would be a cross-subsidiary credential leak.",
  "login_id": null,
  "password": null,
  "country": "usa",
  "fe_applicable": false,
  "expect_success": false,
  "expected": {
    "_comment": "Only what the BE cannot tell us. Identity and session shape come from LoginResult. error_message is the locale-specific text the FE renders; null means assert only that the login was rejected.",
    "error_message": null
  }
}
